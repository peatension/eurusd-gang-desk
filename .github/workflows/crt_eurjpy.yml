name: CRT Engine EUR/JPY

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run CRT Engine
        env:
          TWELVE_DATA_KEY: ${{ abe3c75bfa1c4d9ea0322f1ee42734dd }}
          BOT_TOKEN: ${{ 8853592460:AAEgmScA22uJNulbrMQqywArmprOYW_NHb4 }}
          CHAT_ID: ${{ 1004423692487 }}
        run: python crt_engine_eurjpy.py

      - name: Save state
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add crt_state_eurjpy.json
          git commit -m "Update CRT state [skip ci]" || echo "No changes"
          git push
